{
  "name": "@glacier-network/elizaos-adapter",
  "version": "0.0.1-alpha.1",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@elizaos/core": "0.1.7",
    "@glacier-network/client": "^0.2.1",
    "@tavily/core": "^0.0.2"
  },
  "devDependencies": {
    "tsup": "8.3.5"
  },
  "scripts": {
    "build": "tsup --format esm --dts",
    "dev": "tsup --format esm --dts --watch",
    "lint": "eslint --fix  --cache ."
  }
}
